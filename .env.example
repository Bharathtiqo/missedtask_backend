# Application Settings
SECRET_KEY=scope-secret-key-2024-change-in-production

# Database Configuration
# Choose ONE of the following options:

# Option 1: SQLite (Quick testing - no server needed)
DATABASE_URL=sqlite:///./missedtask.db

# Option 2: MySQL (with MySQL Workbench)
# DATABASE_URL=mysql+pymysql://root:your_password@localhost:3306/missedtask

# Option 3: PostgreSQL
# DATABASE_URL=postgresql://username:password@localhost:5432/missedtask

# For production (Render, Railway, Heroku, etc.):
# DATABASE_URL will be automatically provided by the platform

# API Configuration
API_PORT=8000

# CORS Allowed Origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173

# Frontend Environment Variables (for reference)
# These should be set in your frontend .env file
# REACT_APP_API_URL=http://localhost:8000
# REACT_APP_WS_URL=ws://localhost:8000