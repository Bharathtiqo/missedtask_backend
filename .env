# Application Settings
SECRET_KEY=scope-secret-key-2024-change-in-production

# ============================================================================
# DATABASE CONFIGURATION - READ THIS CAREFULLY!
# ============================================================================
#
# LOCAL DEVELOPMENT (this file):
# Use the DATABASE_URL below for local testing with MySQL on your laptop
#
# DATABASE_URL=mysql+pymysql://root:Bharath%401234@localhost:3306/missedtask
#
# PRODUCTION ON RENDER:
# 1. DO NOT use localhost in production - it won't work!
# 2. Set DATABASE_URL in Render Dashboard → Environment variables
# 3. Use your managed MySQL/PostgreSQL host, NOT localhost
#
# Example for production MySQL (set in Render, NOT here):
# DATABASE_URL=mysql+pymysql://user:password@your-db-host.com:3306/missedtask
#
# Example for production PostgreSQL (Render provides this):
DATABASE_URL=postgresql://missedtask_vx7w_user:QuBVd50zKkgaSRjTiZWynD95WDpN4env@dpg-d3nnj2be5dus73efc03g-a.oregon-postgres.render.com:5432/missedtask_vx7w
#
# ============================================================================

# API Configuration
API_PORT=8000

# CORS Allowed Origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173

# Frontend Environment Variables (for reference)
# These should be set in your frontend .env file
# REACT_APP_API_URL=http://localhost:8000
# REACT_APP_WS_URL=ws://localhost:8000